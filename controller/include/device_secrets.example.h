#pragma once

// Use a different random key for every physical device. Keep it out of git.
// Minimum length: 12 characters. A 32-character random value is recommended.
#define AIRGAP_DEVICE_KEY "replace-with-a-unique-random-device-key"
